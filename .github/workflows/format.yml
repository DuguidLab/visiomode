name: "Check code format"

on:
  push:
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  lint:
    name: "ruff"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/ruff-action@v3
        with:
          src: "./src"
      # - run: ruff check
      # - run: ruff format